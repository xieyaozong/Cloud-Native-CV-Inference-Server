# AWS ECS Deployment

Build the Docker image, push it to ECR, register the task definition, and run it as a Fargate service. Keep model storage and logs outside the container for production deployments.
